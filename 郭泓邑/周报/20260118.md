

# 周报

## 一、本周学习目标

- 从 JavaSE 过渡到 Java Web / Spring Boot
- 理解后端项目的**工程结构**
- 能独立启动一个 Spring Boot 项目
- 理解并实现一个完整的 Web 接口调用流程

---

## 二、已完成内容概览

### 1. JavaSE 基础回顾与巩固

- 掌握 Java 类、对象、方法、封装（getter / setter）
- 理解 `public class` 与文件名必须一致的规则
- 理解 Java 程序入口 `public static void main(String[] args)`
- 学会使用 `javac` / `java` 命令编译与运行程序
- 理解 `ClassNotFoundException` 的本质原因（类路径 / 包名问题）

---

### 2. Spring Boot 项目创建与启动

- 使用 IDEA 创建 Spring Boot 项目
- 理解并补全启动类 `DemoApplication`
- 掌握 Spring Boot 项目标准结构：

```text
src/main/java
 └── com.demo.demo
     ├── DemoApplication.java
     ├── controller
     ├── service
     ├── dao
     └── entity
```

### 3. 第一个 Web 接口（HelloController）

#### Controller 示例

```java
@RestController
public class HelloController {

    @GetMapping("/hello")
    public String hello() {
        return "Hello Spring Boot";
    }
}
```

#### 掌握知识点

- `@RestController` 的作用
- `@GetMapping` 映射 HTTP GET 请求
- 浏览器访问 `http://localhost:8080/hello`
- 理解“接口”与“方法”的对应关系

------

### 4. 三层架构（重点突破）

#### 三层结构定义

| 层级         | 职责                |
| ------------ | ------------------- |
| Controller   | 接收请求 / 返回响应 |
| Service      | 业务逻辑处理        |
| DAO / Mapper | 数据访问            |

------

### 5. 实现标准三层结构 Demo

#### 实体层（Entity）

```java
public class User {
    private Integer id;
    private String name;
    private Integer age;
}
```

#### DAO 层（模拟数据）

```java
@Repository
public class UserDao {
    public User findUserById(Integer id) {
        return new User(id, "Tom", 18);
    }
}
```

#### Service 层

```java
public interface UserService {
    User getUserById(Integer id);
}
@Service
public class UserServiceImpl implements UserService {

    private final UserDao userDao;

    public UserServiceImpl(UserDao userDao) {
        this.userDao = userDao;
    }

    public User getUserById(Integer id) {
        return userDao.findUserById(id);
    }
}
```

#### Controller 层

```java
@RestController
public class UserController {

    private final UserService userService;

    public UserController(UserService userService) {
        this.userService = userService;
    }

    @GetMapping("/user/{id}")
    public User getUser(@PathVariable Integer id) {
        return userService.getUserById(id);
    }
}
```

------

### 6. 请求完整调用链

```text
浏览器请求
   ↓
Controller（接收请求）
   ↓
Service（业务处理）
   ↓
DAO（数据来源）
   ↓
Service
   ↓
Controller
   ↓
JSON 响应
```

- Controller 不直接操作 DAO
- Service 是业务核心
- 分层带来可维护性和可扩展性

------

## 三、当前掌握能力总结

-  能独立创建并启动 Spring Boot 项目
-  能编写并访问 RESTful 接口
-  理解 JSON 数据返回机制
-  掌握 Controller / Service / DAO 三层架构

------

## 四、当前阶段问题与反思

- 对 Spring 注解的底层原理还未深入
- 对数据库访问（MySQL / MyBatis）尚未接入
- 对接口参数、异常处理还未系统学习

------

## 五、下周学习计划（已确认）

1. 接入 MySQL + MyBatis（真实数据库）
2. 接口参数传递 & JSON 请求体
3. 统一异常处理（企业必会）
4. Spring Boot 配置文件（application.yml）

------

## 六、总体学习状态评估

> **已成功完成从 JavaSE → Java Web → Spring Boot 的关键跨越**

------

