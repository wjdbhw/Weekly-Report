------

# 📘 Spring Boot 学习总结

## （接口参数 & JSON 请求体）

------

## 一、接口参数接收（HTTP → Java）

### 1️⃣ 为什么要学接口参数？

后端接口的本质是：

```text
接收请求数据 → 处理 → 返回结果
```

如果不会**接收参数**，接口就没有意义。

------

## 二、HTTP 请求中参数的三种来源（核心）

| 来源           | 示例                | 使用场景        |
| -------------- | ------------------- | --------------- |
| 路径参数       | `/user/1`           | REST 风格       |
| 查询参数       | `?id=1&name=Tom`    | 简单查询        |
| 请求体（JSON） | `{ "name": "Tom" }` | 真实项目（V+6） |

------

## 三、路径参数（`@PathVariable`）

### 示例

```java
@GetMapping("/user/{id}")
public String getUser(@PathVariable Integer id) {
    return "id = " + id;
}
```

### 请求

```text
GET /user/1
```

### 核心机制

- `{id}` 是 URL 占位符
- URL 中的值 → 自动注入方法参数
- Spring 自动完成 **String → Integer** 转换

### 使用场景

- 查询单个资源
- RESTful 风格接口（**推荐**）

------

## 四、查询参数（`@RequestParam`）

### 示例

```java
@GetMapping("/user")
public String getUser(
        @RequestParam Integer id,
        @RequestParam String name
) {
    return "id=" + id + ", name=" + name;
}
```

### 请求

```text
GET /user?id=1&name=Tom
```

### 规则总结

| URL 参数    | 方法参数                    |
| ----------- | --------------------------- |
| `?id=1`     | `@RequestParam Integer id`  |
| `?name=Tom` | `@RequestParam String name` |

### 特点

- 参数写在 `?` 后
- 参数多时不美观
- 不适合复杂结构

------

## 五、内容总结

 Controller 接收外部数据
 Spring MVC 参数绑定
 自动类型转换
 接口不再是“写死的字符串”

------

# JSON 请求体 & `@RequestBody`

> **这是前后端分离的分水岭**

------

## 一、为什么真实项目一定用 JSON？

传统参数方式的问题：

- 参数一多 → 方法爆炸
- 无法表达复杂结构
- 不适合前后端分离

JSON 的优势：

- 结构清晰
- 可嵌套
- 语言无关

👉 **企业接口 80% 使用 JSON**

------

## 二、目标

```text
JSON → Java 对象 → 返回 JSON
```

------

## 三、实体类（Entity / DTO）

### User.java

```java
public class User {

    private String name;
    private Integer age;

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public Integer getAge() {
        return age;
    }

    public void setAge(Integer age) {
        this.age = age;
    }
}
```

### 规则（非常重要）

- 字段名 **必须和 JSON key 一致**
- 必须有 `getter / setter`
- 无需构造方法（默认即可）

------

## 四、`@RequestBody` 接收 JSON

### Controller 示例

```java
@PostMapping("/user")
public User createUser(@RequestBody User user) {
    return user;
}
```

### 请求（HTTP Body）

```json
{
  "name": "Tom",
  "age": 18
}
```

------

## 五、`@RequestBody` 做了什么？

内部流程（面试级理解）：

1. 读取 HTTP Body
2. 使用 **Jackson**
3. JSON → Java 对象（反序列化）
4. 注入方法参数

------

## 六、返回值为什么自动变 JSON？

```java
@RestController
```

=

```java
@Controller + @ResponseBody
```

Spring 自动做：

```text
Java 对象 → JSON（序列化）
```

------

## 

| 参数位置 | URL                               | Body           |
| -------- | --------------------------------- | -------------- |
| 注解     | `@PathVariable` / `@RequestParam` | `@RequestBody` |
| 请求方式 | GET                               | POST           |
| 适用场景 | 简单参数                          | 真实业务       |
| 企业使用 | 少                                | 多             |

------

## 七、当前阶段

```text
Spring Boot
 ├── Controller
 │    ├── @GetMapping
 │    ├── @PostMapping
 │    ├── @PathVariable
 │    ├── @RequestParam
 │    └── @RequestBody  
 └── JSON（Jackson）
```

------

> 